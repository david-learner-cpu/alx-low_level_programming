#include "main.h"
/**
 * _abs - a function that computes absolute value of an integer
 * @abs: the int that will use for the arguement of the function
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0 || < == 0)
	{
		return (n);
	}
	else
	return (n * -1);
}
