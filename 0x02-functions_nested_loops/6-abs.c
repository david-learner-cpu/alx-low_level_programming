#include "main.h"
/**
 * _abs - a function that computes absolute value of an integer
 * @abs: the int to check
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
