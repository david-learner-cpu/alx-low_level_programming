#include <main.h>

/**
 * main - Prints the alphabets in lowercase and followed by a new line
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	int n;
	for (n=a; n <= A; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
